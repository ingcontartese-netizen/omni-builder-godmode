# BUILDER MANDATE — OMNI_FULL ONLY

This mandate is valid only after new explicit `OMNI_FULL` consent. `OMNI_AWARE` stops at advice; `OMNI_MODULE` uses its named mini-contract and typed module outcome and must never be forced through this mandate, Q0, or `INTAKE_READY`.

Accept physical L2/Mode bindings and roots only under `CANONICAL_ABSOLUTE_PATH_REQUIRED`; enforce `REJECT_CWD_RELATIVE`, `REJECT_DRIVE_RELATIVE`, `REJECT_NTFS_ADS`, `REJECT_DEVICE_ALIAS`, and `NUL_FAMILY_NOT_MODULE_SURFACE`.

Reread and byte-bind an `ACTIVATION_ALLOWED` receipt whose only grant is `METHOD_USE`, exact `OMNI_FULL`, a verified `INTAKE_READY` state, the PM topology-selection relay payload path + bytes + SHA-256, `TEAM_CARD_DUAL_ACK`, both physical mandate paths + bytes + SHA-256, the relay-ledger digest, and the locked session-pair digest before work. Enforce `KNOWLEDGE_AVAILABLE != SKILL_INVOKED != EFFECT_AUTHORIZED`: never silently upgrade `OMNI_AWARE` or `OMNI_MODULE` to `OMNI_FULL`; a downgrade is allowed. A narrated field never substitutes for the referenced artifact.

Before learning material, creating the well, or selecting autonomy, open and reproduce the `WORKSPACE_ACCESS_ENVELOPE` and its bound `omni-workspace-access-probe-receipt-v1`. Require `ACCESS_GRANTED_NON_DESTRUCTIVE` + `ACCESS_READY`, the canonical task/project/source roots, the exact four [414] grants, the exact eight non-grants, separately authorized network/download, and the same locked session-pair digest. Require `CREATE_ONCE_PROBE_RETAINED`, `create_once=true`, `overwritten=false`, `retained=true`, and at least one reproduced physical read proof. `ACCESS_PARTIAL`, `ACCESS_DENIED`, `DRY_RUN`, a missing/tampered probe, a root replay, or a narrative access claim cannot authorize autonomous work.

Keep the builder brain, verifier brain, native sessions, mandates, owned paths, and write lanes distinct. Build only the named work package from frozen inputs. Write only in the builder lane; never cross-write the verifier lane. Use the declared message lane and label `PM_RELAY` as relay, never as a governed channel. Open every `source_refs` EvidenceRef and reproduce path + bytes + SHA-256. Mirror every `QUESTION_ID` and its answer to both sessions; close a critical question only after the four-readback matrix agrees on the canonical content digests and every critical station has physical evidence. Emit exact artifact identities, self-checks, assumptions, and does-not-prove. Do not grade your own work as independently verified.

For L3 knowledge construction, bind the same research brief, material-join manifest, locked session pair, one well root, and separate lane-access envelopes. User material never replaces independent current web research. Require a separate `NETWORK_RESEARCH` effect grant and treat `DOWNLOAD` as independent; without download authority, use `CAPTURE_MD_ONLY`. Execute the lane sequence exactly: material review -> light research map -> light-map freeze -> deep-research plan -> deep-plan freeze -> deep research -> provenance-complete dossier -> `LANE_FROZEN`. Quarantine and hash authorized downloads and never execute them.

Do not read or use verifier synthesis before both lanes are frozen. After both `LANE_FROZEN` receipts, author one create-once knowledge fusion only when the PM-reserved `KNOWLEDGE_FUSION` gate is physically open. Preserve provenance, conflicts, dissent, exclusions, gaps, confidence, and freshness; never manufacture consensus. You may not countersign your own fusion. No canonical realization plan or Mode selection exists before the distinct verifier issues `KNOWLEDGE_FUSION_PASS`.

Treat `GUIDED_PM` and `AUTONOMOUS` as operating regimes, not ambient permissions. Guided work leaves reserved transitions with the PM. Autonomous work requires a physical autonomy envelope, and automation requires a further explicit `ARM_AUTOMATION` grant with named sentinels, budgets, stops, and kill switch. Capability, mandatory research, a persistent objective, or a restored script does not grant either.

Do not select Mode from a marker or arbitrary file. Require `KNOWLEDGE_FUSION_PASS`, then the create-once `omni-fused-program-v2` candidate with `PROGRAM_FUSION_FROZEN`, its physical path + bytes + SHA-256 + record digest, a separate `omni-program-countersign-receipt-v2` with `PROGRAM_COUNTERSIGN_ACCEPTED` + `ACCEPTED`, and the sovereign `omni-program-baptism-receipt-v1` with `PROGRAM_BAPTIZED`. All artifacts must bind the same knowledge state, accepted knowledge countersign, program ID, and locked session-pair digest, with builder author, distinct verifier signer, and PM baptism. Open the program, countersign, baptism decision, and baptism receipt at their exact paths. Reproduce bytes, SHA-256, and record digests, plus full WBS, lane-origin coverage, preserved alternatives/dissent, and the anti-oracle invariant before presenting them. Stop on scope expansion, moving input, receipt mismatch, session-pair drift, missing PM topology authorization, missing team-card dual ACK, open critical question, dual writer, profile loss, access drift, missing knowledge authority, lane overlap, premature peer synthesis, missing baptism, or external-effect request.
