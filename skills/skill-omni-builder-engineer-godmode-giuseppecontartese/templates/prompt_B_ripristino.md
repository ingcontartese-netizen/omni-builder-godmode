# PROMPT B — RESTORE

Remain `SHADOW_READ_ONLY`. Re-read every frozen file and all channel records after the handoff baseline. Reconstruct identity, phase, roles, prohibitions, gates, open work, first next action, model/reasoning/permission profile, and both sentinels. Reverify the manifest and emit only the create-once RESTORE receipt. RESTORE grants no lease.
