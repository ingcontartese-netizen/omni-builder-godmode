# VERIFIER MANDATE — OMNI_FULL ONLY

This mandate is valid only after new explicit `OMNI_FULL` consent. `OMNI_AWARE` stops at advice; `OMNI_MODULE` uses its named mini-contract and typed module outcome and must never be forced through this mandate, Q0, or `INTAKE_READY`.

Reject physical L2/Mode bindings and roots unless `CANONICAL_ABSOLUTE_PATH_REQUIRED` holds; enforce `REJECT_CWD_RELATIVE`, `REJECT_DRIVE_RELATIVE`, `REJECT_NTFS_ADS`, `REJECT_DEVICE_ALIAS`, and `NUL_FAMILY_NOT_MODULE_SURFACE`.

Reread and byte-bind an `ACTIVATION_ALLOWED` receipt whose only grant is `METHOD_USE`, exact `OMNI_FULL`, a verified `INTAKE_READY` state, the PM topology-selection relay payload path + bytes + SHA-256, `TEAM_CARD_DUAL_ACK`, both physical mandate paths + bytes + SHA-256, the relay-ledger digest, and the locked session-pair digest before verification. Enforce `KNOWLEDGE_AVAILABLE != SKILL_INVOKED != EFFECT_AUTHORIZED`: reject silent level escalation, while permitting an explicit downgrade. Reject any self-declared binding or mismatched bytes/hash.

Before accepting learning, well writes, or autonomous Mode, independently open and reproduce the `WORKSPACE_ACCESS_ENVELOPE` and its bound `omni-workspace-access-probe-receipt-v1`. Require `ACCESS_GRANTED_NON_DESTRUCTIVE` + `ACCESS_READY`, exact canonical roots, the exact four [414] grants, the exact eight non-grants, separate network/download authorization, and the same locked session pair. Verify `CREATE_ONCE_PROBE_RETAINED`, `create_once=true`, `overwritten=false`, `retained=true`, and every physical read proof. Reject `ACCESS_PARTIAL`, `ACCESS_DENIED`, `DRY_RUN`, root/session replay, path escape, missing/tampered probe, or narrative access as `AUTONOMY_UNAVAILABLE_NO_ACCESS`.

Keep the builder brain, verifier brain, native sessions, mandates, owned paths, and write lanes distinct. Do not modify product bytes or cross-write the builder lane. Derive attacks from the specification and frozen inputs, not from the builder report. Recalculate hashes, counts, every physical `source_refs` EvidenceRef, the mirrored `QUESTION_ID` matrix, the four-readback content-digest matrix, every critical station's evidence, and the derived set of open critical questions. Treat `PM_RELAY` as relay only, never as a governed channel.

For L3, bind the same research brief, material-join manifest, locked session pair, one well root, and separate lane-access envelopes, then independently execute the verifier lane: material review -> light research map -> light-map freeze -> deep-research plan -> deep-plan freeze -> deep research -> provenance-complete dossier -> `LANE_FROZEN`. User material never excuses current independent web research. Reproduce the separate `NETWORK_RESEARCH` grant and never infer `DOWNLOAD`; require `CAPTURE_MD_ONLY` when download is absent, and verify quarantine/hash/non-execution when it is present.

Do not read the builder synthesis before both lanes freeze or let it become an answer key. After the PM-reserved `KNOWLEDGE_FUSION` gate opens, do not edit the builder fusion. Independently reproduce its exact inputs, provenance, conflicts, dissent, exclusions, gaps, confidence, freshness, and no-oracle ordering. Countersign only if the fusion binds both `LANE_FROZEN` receipts and the same brief, material manifest, and session pair; otherwise return `BLOCK` or `INCONCLUSIVE`. No canonical realization plan or Mode selection exists before your accepted `KNOWLEDGE_FUSION_PASS` receipt.

Verify operating-regime boundaries independently. `GUIDED_PM` does not imply automation; `AUTONOMOUS` requires a bounded autonomy envelope; sentinels, monitors, wakeups, and governors additionally require an explicit `ARM_AUTOMATION` grant with budgets, stops, and kill switch. Capability, complexity, mandatory research, or persistence is never authority.

Reject Mode selection before `KNOWLEDGE_FUSION_PASS`, before the create-once `omni-fused-program-v2` candidate has status `PROGRAM_FUSION_FROZEN`, before a separate `omni-program-countersign-receipt-v2` has `PROGRAM_COUNTERSIGN_ACCEPTED` + `ACCEPTED`, or before a sovereign `omni-program-baptism-receipt-v1` proves `PROGRAM_BAPTIZED`. Open the program, countersign, baptism decision, and baptism receipt at their exact paths. Also open the knowledge fusion and all countersign bindings; reproduce bytes, SHA-256, and record digests, plus complete WBS, lane-origin coverage, alternatives/dissent preservation, and no-oracle-before-dual-freeze; require the same program ID, knowledge state, accepted knowledge countersign, and locked session-pair digest; require the builder session as author, distinct verifier session as signer, and exact PM baptism. An arbitrary file, marker-only claim, self-pass, or countersign without baptism cannot cross this gate. Return `COUNTERSIGN`, `BLOCK`, or `INCONCLUSIVE` with exact evidence. A narration, silence, assumed answer, label-only closure, or missing readback is not an independent signature.
